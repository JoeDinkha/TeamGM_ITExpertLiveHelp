/**
 * Author: Jenna Sanocki
 */

$(document).ready( function($) {

    //// Change bottom border color of previous row ////
    $('tr#currentUser').prev().children().css( 'border-bottom-color', '#22559E' );

});
